.TH PRINTF 3 "October 2023" "1.0" "Library Functions"
.SH NAME
_printf \- formatted output conversion
.SH SYNOPSIS
.B #include <stdio.h>
.PP
.B int _printf(const char *format, ...);
.SH DESCRIPTION
The \fB_printf\fR function sends formatted output to the standard output (stdout). It produces output according to a format string that specifies how subsequent arguments (if any) are converted for output.

The format string can contain:
.TP
.B %c
for a character.
.TP
.B %s
for a string.
.TP
.B %d
for signed decimal integers.
.TP
.B %i
for signed integers.
.TP
.B %%
to print a literal percent sign.

.SH RETURN VALUE
The \fB_printf\fR function returns the total number of characters written, or a negative number if an output error occurs.

.SH EXAMPLES
.PP
The following example demonstrates the use of \fB_printf\fR:
.PP
.B #include <stdio.h>
.PP
.B int main() {
    _printf("Hello, World!\n");
    _printf("Character: %c\n", 'A');
    _printf("String: %s\n", "Hello");
    _printf("Integer: %d\n", -123);
    return 0;
}
.SH ERRORS
The \fB_printf \fR function may fail and set errno if:
.TP
.B EINVAL
The format string is invalid.
.TP
.B EIO
An I/O error occurs.

.SH SEE ALSO
.BR sprintf (3),
.BR fprintf (3),
.BR snprintf (3)

.SH AUTHOR
Written by [batrivieredev] <baptiste.rivierefr@gmail.com>
