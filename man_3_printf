.TH _PRINTF 3 "November 2024" "holbertonschool-printf" "_PRINTF MANUAL"
.SH NAME
_printf \- formatted output conversion

.SH SYNOPSIS
.B #include <stdarg.h>
.sp
.B int _printf(const char *format, ...);

.SH DESCRIPTION
.B _printf
is a custom implementation of the standard C library function
.B printf
used to produce output according to a format string.

The format string contains both characters and conversion specifiers. Characters are printed as-is. Conversion specifiers, introduced by the
.B %
character, specify how the arguments should be formatted and printed.

The supported conversion specifiers are:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a string. If the string argument is NULL, the string
.B (null)
is printed.

.TP
.B %%
Prints a literal percent sign.

.SH RETURN VALUE
.B _printf
returns the total number of characters printed (excluding the null byte used to end output strings). If the
.I format
string is NULL, the function returns
.BR \-1 .

.SH EXAMPLES
.TP
Print a string:
.RS
.nf
_printf("Hello, %s!\\n", "world");
.fi
Output:
.RS
Hello, world!
.RE

.TP
Print a character:
.RS
.nf
_printf("Character: %c\\n", 'A');
.fi
Output:
.RS
Character: A
.RE

.TP
Print a literal percent sign:
.RS
.nf
_printf("100%% complete\\n");
.fi
Output:
.RS
100% complete
.RE

.SH BUGS
The
.B _printf
function does not handle:
- Field width
- Precision
- Length modifiers
- Flags

.SH AUTHOR
Written by Holberton School students.

.SH SEE ALSO
.BR printf (3),
.BR write (2)
