.TH _PRINTF 3 "November 2024" "1.0" "Library Functions"
.SH NAME
_printf \- a simple version of the printf function

.SH SYNOPSIS
#include <unistd.h>
#include <stdarg.h>
#include <stdio.h>
#include "main.h"
#include <stdint.h>

int _printf(const char *format, ...);

.SH DESCRIPTION
The \fB_printf\fR function is a simplified version of the standard \fBprintf\fR function. It formats and prints data to the standard output (stdout) based on a format string.

.SH PARAMETERS
.TP
\fBconst char *format\fR
The format string that specifies how subsequent arguments (if any) are converted for output. It may contain ordinary characters (which are printed as-is) and format specifiers that begin with a '%' character.

.SH RETURN VALUE
The \fB_printf\fR function returns the total number of characters printed (excluding the null byte used to end output to strings). If an error occurs, it returns \-1.

.SH ERRORS
.TP
.B EINVAL
The \fBformat\fR parameter is NULL.

.SH EXAMPLES
.nf
#include <stdio.h>

int main() {
    _printf("Hello, World!\n");
    _printf("Number: %d\n", 42);
    return 0;
}
.fi

.SH SEE ALSO
printf(3)

.SH AUTHOR
Written by [Baptiste RIVIERE].
